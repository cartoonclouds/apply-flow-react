export enum ApplicationAttendanceType {
  Remote = "remote",
  Hybrid = "hybrid",
  OnSite = "on-site",
}

export enum ApplicationEmploymentType {
  PartTime = "part-time",
  Contract = "contract",
  Internship = "internship",
  FullTime = "full-time",
  Volunteer = "volunteer",
}
